# Onboarding V2：师兄的问题 → 论文现在的回答（2026-08）

师兄上次提的问题，每一个现在论文里都有了实证回答。这份文档逐题对账：问题是什么、我们怎么答的、关键数字、在论文哪一节。

---

## Q1. 为什么区分 formation / preservation 很重要？

**一句话答案：两种失败需要的补救方向相反，分不清就浪费预算。**

- Formation failure（信号从未形成）→ 需要注入知识：训练、检索增强、知识编辑。轻量干预对它无效。
- Preservation failure（信号形成后被压制）→ 只需要保住已有知识：轻量推理时干预就够。

**实证支撑（论文 §7，因果链）：**

| 干预方法 | preservation 恢复率 | formation 恢复率 | 差距 |
|---|---|---|---|
| Gold-direction patch | 7.3% | 0.7% | 10.9×（p=0.005）|
| Peak-state restoration (α=1) | 12.3% | 1.8% | 7.0× |
| RepE steering | 8.6% | 4.3% | 2.0× |

这正是师兄说的"有一些 case 你就算 intervene 也没有用，纯粹浪费时间"：**formation 在所有干预下恢复率都极低，对它干预就是浪费**。识别出来直接省掉。

**预算视角（最有说服力的一段）**：无差别干预每次恢复 5.7%；用预测器做 trigger、只干预预测为 preservation 的前 20%，每次恢复 9.5%（**+67% 效率，省 80% 预算**）。

---

## Q2. 为什么这两个很难区分？

**一句话答案：输出完全一样错，而领域原有的判据全是伪判据。**

三块实证（论文 §5.1 和 §3.3，互相印证）：

1. **"中间层曾超过错误输出"是伪判据**：92.2% 的错误满足，但把竞争者换成随机 token，98.8% 也满足；换成强度匹配的 token，98.7% 也满足。→ 几乎任何 token 都会被"穿越"，这个判据无信息量。
2. **"任一答案 token 曾进 top-5"（min 规则）是伪判据**：1494 个错误上给 90.6%，但严格 ALL 规则（每个 token 同层进 top-5）只有 13.2%。token 越多越容易碰中一个。
3. **读出未校准会夸大 2 倍**：raw lens 直接投到词表会高估中间层 margin（4.53 vs 校准后 2.24）。

**所以需要**：严格判据（Eq.8：rank≤5 且 margin>0）+ tuned lens 校准 + null 对照。这是论文的方法学贡献。

---

## Q3. 拓展到更多场景（不只是 QA/推理）

已从 3 任务扩到 **7 任务**，覆盖 7 种能力类型（论文 Table 3）：

| 任务 | 类型 | 严格 ALL 率 |
|---|---|---|
| HotpotQA | 多跳推理 | 24.2% |
| TriviaQA | 知识问答 | 23.0% |
| MMLU | 世界知识 | 16.8% |
| ARC-Challenge | 科学推理 | 12.2% |
| CommonsenseQA | 常识推理 | 9.7% |
| GSM8K | 数学推理 | 4.3% |
| QuALITY | 长文本理解 | 0.5% |

**新 insight（这轮最有意思的发现）**：具有序列级完整性的 preservation 集中在**短答案实体型任务**；长答案任务（QuALITY）的信号形成是**碎片化的**（首 token 能形成 12.1%，全序列几乎从不 0.5%）；数学近乎没有（程序性计算要么对、要么从未形成）。

---

## Q4. 用场景角度回答 Q1/Q2（师兄的联动提议）

**场景版"为什么重要"**：preservation 比例跨任务差 **48 倍**（24.2% vs 0.5%）。意味着"要不要部署干预"高度依赖场景：知识问答类值得（1/4 的错误可挽救），数学类不值得（干预基本无效），这直接就是部署决策依据。

**场景版"为什么难"**：场景本身会制造测量假象。同一个 QuALITY，宽松判据说 27.6%（第 2 高）、严格判据说 0.5%（垫底），**差 55 倍、结论完全反转**。不校准的判据在不同场景会给出方向性错误的结论 → 这就是为什么严格的测量框架是必需品而不是细节。

这两段联动写在论文 Intro 第 1 段和 §5.4。

---

## Q5. 预测的目标到底是什么？

明确回答：**预测对象是"这个错误的 gold 信号轨迹后续会不会衰减"，不是"模型会不会答错"。**

- 我们只分析**已经答错**的样本（预测答对答错是另一个问题）
- 标签定义（论文 §6 开头）：`y_decay = 1` 当 gold 的 margin 在后半段某层曾为正、但最终层为负（即"后半段会发生形成后衰减"）
- 输入：**只用前半段层的特征**（严格时间分离，标签只用后半段信息）
- 结果：线性分类器 AUC **0.75 ± 0.02**（5-fold CV，全文唯一 headline）；RF 0.92 作为上界
- 附加：endpoint-free 版（特征完全不引用最终竞争者）0.66，排除"偷看终点"

另有一个部署用的预测目标：**preservation vs formation 二分类**（当 trigger 用），非 oracle 预测器 AUC 0.669（只用模型自己的 top-5 动态，不需要知道正确答案）。

---

## Q6. Insights 指导已有 intervene（师兄的核心提议，已全部实现）

师兄原话的意思拆成三条，我们全做了，而且**没有提出任何新干预方法**（用的都是已有的：RepE steering、logit bonus）：

**① 预测到衰减 → 用已有方法干预 → 看能不能恢复**
用轨迹预测器当 trigger，触发 RepE steering：预测为 preservation 的样本干预后恢复 8.6%，formation 只有 4.3%（2.0×）→ 干预的价值被预测器精准定位了。

**② 当 trigger 省预算**
无差别干预 5.7%/次 vs 触发式（top 20%）9.5%/次 = **+67% 效率增益，省 80% 预算**。oracle 辅助版 trigger 更高（+127%）。论文原话已明确写了这是 offline oracle-assisted validation，不吹成部署方法。

**③ "有的 case 干预也没用"的量化**
formation 在 logit bonus（6.4%）、RepE（4.3%）、gold patch（0.7%）下恢复率全部显著低于 preservation → 这类错误该走训练/检索路线，干预是浪费时间。**taxonomy 的实用价值就是把这两条路线分开。**

这一整块在论文 §7 "Calibrated Steering Validates the Taxonomy" + "Closing the loop" 段落。

---

## 给师兄的快速索引

| 师兄的问题 | 论文位置 | 核心数字 |
|---|---|---|
| 为什么重要 | Intro ¶1 + §7 | 10.9× 差异修复性；+67% 效率/省 80% 预算 |
| 为什么难 | Intro ¶2 + §5.1 + §3.3 | 92.2% vs 98.8% null；90.6% vs 13.2% |
| 更多场景 | §5.4 Table 3 | 7 任务，24.2% → 0.5% |
| 场景角度联动 | Intro + §5.4 | QuALITY 55 倍反转 |
| 预测目标 | §6 开头 | y_decay 定义；0.75（CV headline）|
| insights 指导 intervene | §7 闭环段 | 2.0× 差异 + 64% 触发精度 |

---

## 当前待办（师兄可帮忙判断的）

1. main.tex 正文页数超标 2-3 页 → 优先压哪里
2. 可复现性：匿名代码链接 + 随机种子表
3. 跨模型严格率（目前严格 Eq.8 只有 Qwen3-8B）
4. 非 oracle 预测器口径：pooled 0.669 vs within-task（内部审计发现 pooled 有任务身份成分，两种口径待定稿）
